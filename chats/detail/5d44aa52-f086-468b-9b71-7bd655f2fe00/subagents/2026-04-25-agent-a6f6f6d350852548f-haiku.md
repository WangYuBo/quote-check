# SubAgent 对话记录 (haiku)

> 项目: quote-check
> Session: 5d44aa52-f086-468b-9b71-7bd655f2fe00
> 时区: Asia/Shanghai (+08:00)

---

## 👤 User [15:38:54 +08:00]

I need to investigate a bug in /Users/wangyubo/github-resp/quote-check.

**The bug**: On the citation verification page (引用核查页面), each citation card always shows "未在参考中找到" (not found in references) even when the citation IS found in the reference list with "完全符合" (fully matches) or "部分符合" (partially matches) status. The statistics counters for 符合参考 / 部分符合 / 不符合 / 未找到 are wrong as a result.

**Investigate these in parallel**:

1. **What is this project?** Look at package.json, README, and overall structure. Tell me: Next.js App Router? framework version? UI library?

2. **The verification page UI**: Find the page that displays citation cards with the "未在参考中找到" / "符合参考" / "部分符合" / "不符合" labels. Likely under `app/` or `src/app/`. Identify:
   - The page file path
   - The component(s) that render each card
   - The exact code/conditions that decide which label/status to show
   - The exact code/conditions that compute the four statistics counters

3. **The verification logic / data source**: Trace where the citation "match status" data comes from. Likely:
   - An API route under `app/api/` that runs the quote check
   - A library function (LLM call? text matching?) that returns per-citation match results
   - Data shape: what field/enum represents the match status? what values can it take?

4. **Where is the disconnect?** Compare:
   - What status values the backend/library returns
   - What status values the frontend checks against in if/switch statements
   - Are there enum mismatches (e.g., backend returns "matched" but frontend checks "fully_matched")? String vs number? Boolean inversion? Default fallback to "not found" when the actual value isn't recognized?

Report with **exact file paths, line numbers, and short code snippets** showing the suspected mismatch. I need enough to write a precise fix plan, not a generic "investigate further". Under 500 words.

---

## 🤖 Assistant [15:38:57 +08:00]

I'll investigate this bug systematically by exploring the project structure, finding the citation verification page, and tracing the data flow.

---

## 🤖 Assistant [15:38:57 +08:00]

### 🔧 Bash

```json
{
  "command": "find /Users/wangyubo/github-resp/quote-check -type f -name \"package.json\" | head -5"
}
```

📤 Result:

```
error: unexpected argument '-n' found

  tip: to pass '-n' as a value, use '-- -n'

Usage: rtk find [OPTIONS] <PATTERN> [PATH]

For more information, try '--help'.
```

---

## 🤖 Assistant [15:39:00 +08:00]

Let me use the correct find syntax:

---

## 🤖 Assistant [15:39:00 +08:00]

### 🔧 Bash

```json
{
  "command": "find /Users/wangyubo/github-resp/quote-check -type f -name \"package.json\""
}
```

❌ Error:

```
Exit code 2
error: unexpected argument '-n' found

  tip: to pass '-n' as a value, use '-- -n'

Usage: rtk find [OPTIONS] <PATTERN> [PATH]

For more information, try '--help'.
```

---

## 🤖 Assistant [15:39:03 +08:00]

Let me try a different approach:

---

## 🤖 Assistant [15:39:04 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la /Users/wangyubo/github-resp/quote-check/ | head -20"
}
```

📤 Result:

```
.42cog/
.42plugin/
.claude/
.git/
.next/
.vercel/
app/
chats/
components/
docs/
e2e/
inngest/
lib/
logs/
node_modules/
notes/
origin/
prompts/
public/
scripts/
```

---

## 🤖 Assistant [15:39:06 +08:00]

Good! Now let me read the key files in parallel:

---

## 🤖 Assistant [15:39:06 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/package.json"
}
```

📤 Result:

```
1	{
2	  "name": "quote-check",
3	  "version": "1.0.0-scaffold",
4	  "private": true,
5	  "description": "文史类引用校对软件 v1.0 — 三维度核查（字词/解释/语境），编辑决定、机器不判错",
6	  "engines": {
7	    "node": ">=20.11"
8	  },
9	  "packageManager": "bun@1.1.38",
10	  "scripts": {
11	    "dev": "next dev",
12	    "build": "next build",
13	    "start": "next start",
14	    "typecheck": "tsc --noEmit",
15	    "lint": "eslint .",
16	    "lint:fix": "eslint . --fix",
17	    "format": "prettier --write .",
18	    "format:check": "prettier --check .",
19	    "test": "vitest run",
20	    "test:watch": "vitest",
21	    "test:coverage": "vitest run --coverage",
22	    "test:unit": "vitest run tests/unit",
23	    "test:contract": "DOCKER_HOST=${DOCKER_HOST:-unix:///Users/wangyubo/.docker/run/docker.sock} vitest run tests/contract",
24	    "test:compliance": "vitest run tests/compliance",
25	    "test:prompts": "vitest run tests/compliance/prompt-integrity.test.ts",
26	    "test:neutral": "vitest run tests/compliance/neutral...(truncated)
```

---

## 🤖 Assistant [15:39:06 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la /Users/wangyubo/github-resp/quote-check/app/"
}
```

📤 Result:

```
api/
history/
login/
reports/
tasks/
upload/
globals.css  2.5K
layout.tsx  737B
page.tsx  1.1K

📊 3 files, 6 dirs (2 .tsx, 1 .css)
```

---

## 🤖 Assistant [15:39:06 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/README.md"
}
```

📤 Result:

```
1	# AI原生项目模板
2	
3	这是一个基于认知敏捷法(42COG)的AI原生项目初始化模板。
4	
5	## 目录结构
6	
7	```
8	projectname/
9	├── .42cog/              # 认知敏捷法核心文件
10	│   ├── meta/            # 项目元信息
11	│   ├── real/            # 现实约束
12	│   ├── cog/             # 认知模型
13	│   ├── spec/            # 规约文档
14	│   ├── work/            # 工作记录
15	│   └── others/          # 其他文档
16	├── .42plugin/           # 活水插件技能库 ⭐
17	│   └── 42edu/           # 教育版11个技能（技能安装位置）
18	│       ├── user-simulation/           # 用户模拟使用
19	│       ├── user-admin/                # 管理员功能
20	│       ├── pm-product-requirements/   # 产品需求文档
21	│       ├── pm-user-story/             # 用户故事
22	│       ├── dev-system-architecture/   # 系统架构设计
23	│       ├── dev-database-design/       # 数据库设计
24	│       ├── dev-coding/                # 实际编码
25	│       ├── dev-quality-assurance/     # 质量保证
26	│       ├── dev-deployment/            # 部署运维
27	│       ├── design-ui-design/          # 交互设计
28	│       └── design-graphic-design/     # 平面设计
29	├── notes/               #...(truncated)
```

---

## 🤖 Assistant [15:39:08 +08:00]

Now let me search for the citation verification page and related components:

---

## 🤖 Assistant [15:39:09 +08:00]

### 🔧 Bash

```json
{
  "command": "find /Users/wangyubo/github-resp/quote-check/app -type f -name \"*.tsx\" -o -name \"*.ts\" | grep -E \"(verif|check|citation|引用)\" | head -20"
}
```

📤 Result:

```
error: unexpected argument '-n' found

  tip: to pass '-n' as a value, use '-- -n'

Usage: rtk find [OPTIONS] <PATTERN> [PATH]

For more information, try '--help'.
```

---

## 🤖 Assistant [15:39:11 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la /Users/wangyubo/github-resp/quote-check/app/tasks/"
}
```

📤 Result:

```
[id]/

📊 0 files, 1 dirs
```

---

## 🤖 Assistant [15:39:13 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la \"/Users/wangyubo/github-resp/quote-check/app/tasks/[id]/\""
}
```

📤 Result:

```
page.tsx  5.1K

📊 1 files, 0 dirs (1 .tsx)
```

---

## 🤖 Assistant [15:39:15 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/tasks/[id]/page.tsx"
}
```

📤 Result:

```
1	'use client';
2	
3	import { PauseCircle, ShieldOff } from 'lucide-react';
4	import { useRouter } from 'next/navigation';
5	import { use, useEffect, useState } from 'react';
6	
7	interface TaskStatus {
8	  taskId: string;
9	  displayId: string;
10	  status: string;
11	  totalQuotes: number | null;
12	  verifiedQuotes: number;
13	  failedQuotes: number;
14	}
15	
16	const STATUS_LABELS: Record<string, string> = {
17	  PENDING_PARSE: '等待中…',
18	  VERIFYING: '正在核查…',
19	  PAUSED_COST: '已暂停（成本预警）',
20	  REJECTED_BY_MODERATION: '内容审核未通过',
21	  COMPLETED: '核查完成',
22	  CANCELED: '已取消',
23	};
24	
25	export default function TaskPage({ params }: { params: Promise<{ id: string }> }) {
26	  const { id } = use(params);
27	  const router = useRouter();
28	  const [task, setTask] = useState<TaskStatus | null>(null);
29	  const [error, setError] = useState('');
30	
31	  useEffect(() => {
32	    let active = true;
33	
34	    async function poll() {
35	      while (active) {
36	        try {
37	        ...(truncated)
```

---

## 🤖 Assistant [15:39:17 +08:00]

Now let me look at the reports page which shows the citation verification results:

---

## 🤖 Assistant [15:39:18 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la \"/Users/wangyubo/github-resp/quote-check/app/reports/\""
}
```

📤 Result:

```
[taskId]/

📊 0 files, 1 dirs
```

---

## 🤖 Assistant [15:39:19 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la \"/Users/wangyubo/github-resp/quote-check/app/reports/[taskId]/\""
}
```

📤 Result:

```
page.tsx  9.7K

📊 1 files, 0 dirs (1 .tsx)
```

---

## 🤖 Assistant [15:39:22 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/reports/[taskId]/page.tsx"
}
```

📤 Result:

```
1	import { notFound } from 'next/navigation';
2	
3	import { auth } from '@/lib/auth';
4	import { getReport } from '@/lib/services/task';
5	import { headers } from 'next/headers';
6	
7	interface ReferenceHit {
8	  referenceId: string;
9	  canonicalName: string;
10	  versionLabel: string | null;
11	  hit: boolean;
12	  snippet: string | null;
13	  similarity: string | null;
14	}
15	
16	interface VerifyResult {
17	  id: string;
18	  quoteId: string;
19	  quoteText: string;
20	  sourceWorkHint: string | null;
21	  matchStatus: 'MATCH' | 'PARTIAL_MATCH' | 'NOT_MATCH' | 'NOT_FOUND_IN_REF';
22	  verdictTextAccuracy: {
23	    verdict: string;
24	    explanation: string;
25	    suggestedCorrection?: string;
26	  };
27	  verdictInterpretation: {
28	    verdict: string;
29	    explanation: string;
30	  };
31	  verdictContext: {
32	    verdict: string;
33	    explanation: string;
34	  };
35	  confidence: string;
36	  confidenceBreakdown: {
37	    refHit: number;
38	    locationValid: number;
39	  ...(truncated)
```

---

## 🤖 Assistant [15:39:24 +08:00]

Good! Now let me check the API that processes the verification and the backend library logic:

---

## 🤖 Assistant [15:39:24 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la \"/Users/wangyubo/github-resp/quote-check/app/api/\""
}
```

📤 Result:

```
admin/
agreement/
auth/
inngest/
manuscripts/
references/
reports/
tasks/

📊 0 files, 8 dirs
```

---

## 🤖 Assistant [15:39:26 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la \"/Users/wangyubo/github-resp/quote-check/app/api/tasks/\""
}
```

📤 Result:

```
[id]/
history/
route.ts  2.9K

📊 1 files, 2 dirs (1 .ts)
```

---

## 🤖 Assistant [15:39:28 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/api/tasks/route.ts"
}
```

📤 Result:

```
1	import type { NextRequest } from 'next/server';
2	import { NextResponse } from 'next/server';
3	
4	import { inngest } from '@/inngest/client';
5	import { auth } from '@/lib/auth';
6	import {
7	  COST_CONFIRM_THRESHOLD_FEN,
8	  DEFAULT_COST_CEILING_FEN,
9	  estimateCostFen,
10	  formatFenAsYuan,
11	} from '@/lib/ai/cost';
12	import { getManuscript } from '@/lib/services/manuscript';
13	import { createTask, updateTaskStatus } from '@/lib/services/task';
14	
15	export async function POST(req: NextRequest) {
16	  const session = await auth.api.getSession({ headers: req.headers });
17	  if (!session?.user?.id) {
18	    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
19	  }
20	
21	  let body: { manuscriptId?: string; referenceIds?: string[]; costConfirmed?: boolean };
22	  try {
23	    body = (await req.json()) as typeof body;
24	  } catch {
25	    return NextResponse.json({ error: '无效 JSON' }, { status: 400 });
26	  }
27	
28	  if (!body.manuscriptId) {
29	    return ...(truncated)
```

---

## 🤖 Assistant [15:39:28 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la \"/Users/wangyubo/github-resp/quote-check/app/api/tasks/[id]/\""
}
```

📤 Result:

```
route.ts  1005B

📊 1 files, 0 dirs (1 .ts)
```

---

## 🤖 Assistant [15:39:30 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/api/tasks/[id]/route.ts"
}
```

📤 Result:

```
1	import type { NextRequest } from 'next/server';
2	import { NextResponse } from 'next/server';
3	
4	import { auth } from '@/lib/auth';
5	import { getTask } from '@/lib/services/task';
6	
7	export async function GET(req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
8	  const session = await auth.api.getSession({ headers: req.headers });
9	  if (!session?.user?.id) {
10	    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
11	  }
12	
13	  const { id } = await params;
14	  const taskRow = await getTask(id);
15	  if (!taskRow) return NextResponse.json({ error: '任务不存在' }, { status: 404 });
16	  if (taskRow.userId !== session.user.id) {
17	    return NextResponse.json({ error: 'Forbidden' }, { status: 403 });
18	  }
19	
20	  return NextResponse.json({
21	    taskId: taskRow.id,
22	    displayId: taskRow.displayId,
23	    status: taskRow.status,
24	    totalQuotes: taskRow.totalQuotes,
25	    verifiedQuotes: taskRow.verifiedQuotes,
26	    failedQuotes: ...(truncated)
```

---

## 🤖 Assistant [15:39:32 +08:00]

Now let me search for the verification/proofread logic in the inngest event handlers:

---

## 🤖 Assistant [15:39:33 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la \"/Users/wangyubo/github-resp/quote-check/inngest/\""
}
```

📤 Result:

```
functions/
client.ts  503B
events.ts  1020B

📊 2 files, 1 dirs (2 .ts)
```

---

## 🤖 Assistant [15:39:34 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la \"/Users/wangyubo/github-resp/quote-check/inngest/functions/\""
}
```

📤 Result:

```
.gitkeep  0B
cost-guard.ts  1.6K
ping.ts  581B
proofread-run.ts  21.3K
ttl-destroy.ts  2.8K

📊 5 files, 0 dirs (4 .ts, 1 .gitkeep)
```

---

## 🤖 Assistant [15:39:36 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/inngest/functions/proofread-run.ts"
}
```

📤 Result:

```
1	import { generateText } from 'ai';
2	import { z } from 'zod';
3	
4	import { inngest } from '@/inngest/client';
5	import { proofreadRequestedEvent } from '@/inngest/events';
6	import { logger } from '@/lib/logger';
7	import { DEFAULT_GENERATION_OPTIONS, MODEL_ID, defaultModel } from '@/lib/ai/client';
8	import {
9	  CONFIDENCE_ALGO_VERSION,
10	  SIMILARITY_MATCH_THRESHOLD,
11	  SIMILARITY_PARTIAL_THRESHOLD,
12	  computeConfidence,
13	} from '@/lib/ai/confidence';
14	import { computeCostFen } from '@/lib/ai/cost';
15	import { isModerationRejection } from '@/lib/ai/moderation';
16	import { retrievePassagesForQuote } from '@/lib/ai/retrieval';
17	import { PROMPT_VERSION, loadPromptRaw } from '@/lib/ai/prompts';
18	import { buildResultIdempotencyKey } from '@/lib/idempotency';
19	import { listUserReferences } from '@/lib/services/reference';
20	import {
21	  createReportSnapshot,
22	  getReport,
23	  getTask,
24	  getTaskContext,
25	  saveQuotes,
26	  saveReferenceHits,
27	  saveVerificat...(truncated)
```

---

## 🤖 Assistant [15:39:38 +08:00]

Perfect! Now I've found the issue. Let me verify by checking the database schema to understand the data types:

---

## 🤖 Assistant [15:39:39 +08:00]

### 🔧 Bash

```json
{
  "command": "find /Users/wangyubo/github-resp/quote-check/lib -type f -name \"*.ts\" | grep -i schema"
}
```

📤 Result:

```
error: unexpected argument '-n' found

  tip: to pass '-n' as a value, use '-- -n'

Usage: rtk find [OPTIONS] <PATTERN> [PATH]

For more information, try '--help'.
```

---

## 🤖 Assistant [15:39:46 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la /Users/wangyubo/github-resp/quote-check/lib/"
}
```

📤 Result:

```
ai/
auth/
corpus/
db/
parsers/
services/
storage/
text/
validations/
auth-client.ts  177B
auth.ts  2.6K
env.ts  2.0K
idempotency.ts  2.4K
logger.ts  809B

📊 5 files, 9 dirs (5 .ts)
```

---

## 🤖 Assistant [15:39:49 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la /Users/wangyubo/github-resp/quote-check/lib/db/"
}
```

📤 Result:

```
migrations/
.gitkeep  0B
index.ts  622B
schema.ts  24.4K
types.ts  2.5K

📊 4 files, 1 dirs (3 .ts, 1 .gitkeep)
```

---

## 🤖 Assistant [15:39:51 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/lib/db/schema.ts",
  "limit": 300
}
```

📤 Result:

```
1	/**
2	 * Drizzle schema for quote-check v1.0
3	 * - 7 核心业务实体（cog.md）
4	 * - 4 辅助表（result_reference_hit / report_snapshot / audit_log / user_agreement_acceptance）
5	 * - 3 Better Auth 表（session / account / verification）
6	 * - 1 版本登记表（prompt_version，ADR-012）
7	 *
8	 * spec-database-design §4.1 · ADR-003/006/011/012 · real.md #7 · notes #6/#7
9	 */
10	
11	import { relations } from 'drizzle-orm';
12	import {
13	  bigint,
14	  boolean,
15	  index,
16	  integer,
17	  jsonb,
18	  numeric,
19	  pgEnum,
20	  pgTable,
21	  serial,
22	  text,
23	  timestamp,
24	  uniqueIndex,
25	  uuid,
26	  varchar,
27	} from 'drizzle-orm/pg-core';
28	
29	/* ─────────────────────────────────────────────────
30	 * Enums
31	 *
32	 * 策略（§4.3 D-03 改进）：
33	 *   - 低演进概率 → pgEnum（user_role / reference_role / quote_kind / match_status）
34	 *   - 高演进概率 → varchar + CHECK + Zod（task.status；见 0001_triggers.sql C-03）
35	 *   - jsonb 内部字段 → 不落 DB，由 Zod 独家守护（三维度 verdict）
36	 * ───────────────────────────────────────────────...(truncated)
```

---

## 🤖 Assistant [15:39:53 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/lib/db/schema.ts",
  "offset": 300,
  "limit": 150
}
```

📤 Result:

```
300	);
301	
302	export const task = pgTable(
303	  'task',
304	  {
305	    id: uuid('id').primaryKey().defaultRandom(),
306	    userId: uuid('user_id')
307	      .notNull()
308	      .references(() => user.id, { onDelete: 'cascade' }),
309	    manuscriptId: uuid('manuscript_id')
310	      .notNull()
311	      .references(() => manuscript.id, { onDelete: 'restrict' }),
312	    displayId: varchar('display_id', { length: 32 }).notNull(),
313	    referenceIds: uuid('reference_ids').array().notNull().default([]),
314	    status: varchar('status', { length: 32 })
315	      .$type<TaskStatus>()
316	      .default('PENDING_PARSE')
317	      .notNull(),
318	    costEstimatedCents: integer('cost_estimated_cents'),
319	    costActualCents: integer('cost_actual_cents'),
320	    costCeilingCents: integer('cost_ceiling_cents'),
321	    costConfirmedAt: timestamp('cost_confirmed_at', { withTimezone: true }),
322	    costConfirmedBy: uuid('cost_confirmed_by').references(() => user.id),
323	    totalQu...(truncated)
```

---

## 🤖 Assistant [15:40:08 +08:00]

Perfect! Now I have all the information I need. Let me create a comprehensive report.

---

## BUG INVESTIGATION REPORT

### 1. PROJECT OVERVIEW
**Tech Stack:**
- **Framework:** Next.js 16.2.4 (App Router)
- **UI Library:** Tailwind CSS 4.2.2 (no shadcn/ui)
- **Runtime:** Bun 1.1.38
- **Database:** PostgreSQL with Drizzle ORM 0.36.4
- **AI SDK:** Vercel AI SDK (ai 3.4.33) + SiliconFlow LLM

---

### 2. CITATION VERIFICATION PAGE IDENTIFIED

**Page Path:** `/Users/wangyubo/github-resp/quote-check/app/reports/[taskId]/page.tsx`

**Card Rendering:** `VerifyCard` component (line 79-151)
- Shows label via `MATCH_LABEL` lookup (lines 46-51)
- **Key line 95:** `{MATCH_LABEL[result.matchStatus] ?? result.matchStatus}`

**Statistics Counters (lines 242-261):** Four counters aggregate results by `matchStatus` enum values:
```typescript
['符合参考', results.filter((r) => r.matchStatus === 'MATCH').length],
['部分符合', results.filter((r) => r.matchStatus === 'PARTIAL_MATCH').length],
['不符合', results.filter((r) => r.matchStatus === 'NOT_MATCH').length],
['未找到', results.filter((r) => r.matchStatus === 'NOT_FOUND_IN_REF').length],
```

---

### 3. THE MISMATCH FOUND

**Database Schema** (`/Users/wangyubo/github-resp/quote-check/lib/db/schema.ts`, lines 50-55):
```typescript
export const matchStatusEnum = pgEnum('match_status_enum', [
  'MATCH',
  'PARTIAL_MATCH',
  'NOT_MATCH',
  'NOT_FOUND_IN_REF',
]);
```
Verification result table (line 366) stores: `matchStatus: matchStatusEnum('match_status')`

**Backend normalization** (`/Users/wangyubo/github-resp/quote-check/inngest/functions/proofread-run.ts`, lines 47-62):
- LLM returns Chinese strings: "一致", "部分", "不一致", "无需比对", etc.
- Function `normMatchStatus()` converts to: `'match' | 'partial' | 'mismatch' | 'not_found' | 'not_applicable'` (lowercase)

**The Bug** (lines 92-97):
```typescript
function llmMatchToDb(status: string): 'MATCH' | 'PARTIAL_MATCH' | 'NOT_MATCH' | 'NOT_FOUND_IN_REF' {
  if (status === 'match') return 'MATCH';
  if (status === 'partial') return 'PARTIAL_MATCH';
  if (status === 'mismatch') return 'NOT_MATCH';
  return 'NOT_FOUND_IN_REF';  // ← DEFAULT FALLBACK
}
```

**When it occurs** (line 406):
```typescript
const finalMatchStatus = preMatchStatus ?? llmMatchToDb(verifyResult.text_accuracy.match_status);
```

The problem: `verifyResult.text_accuracy.match_status` comes from `MatchStatusCoerce` (line 64), which is the normalized lowercase string. When the LLM returns an unrecognized Chinese variant or parsing fails, `normMatchStatus()` returns `'not_found'` (line 52). Then `llmMatchToDb('not_found')` doesn't match any of the first three conditions, so it falls through to the default `return 'NOT_FOUND_IN_REF'`.

**Second issue:** When no references are uploaded (`hasUploadedRefs === false`), `preMatchStatus` is undefined (line 301), so the backend always applies `llmMatchToDb()`, but there's no catch for LLM response formats that slip through normalization. Any Chinese text the LLM returns that isn't explicitly checked in lines 52-60 defaults to "not found".

---

### 4. EXACT LOCATIONS & CODE SNIPPETS

| File | Lines | Issue |
|------|-------|-------|
| `inngest/functions/proofread-run.ts` | 47-62 | `normMatchStatus()`: catches common Chinese variants but incomplete for edge cases |
| `inngest/functions/proofread-run.ts` | 92-97 | `llmMatchToDb()`: no explicit case for `'not_found'`, falls to `NOT_FOUND_IN_REF` default |
| `inngest/functions/proofread-run.ts` | 64 | `MatchStatusCoerce` schema: applies normalization but silent fallback to `'not_found'` |
| `app/reports/[taskId]/page.tsx` | 242-261 | Frontend counters filter by exact enum value—works correctly once backend assigns correctly |

**Root cause:** When LLM text_accuracy verdict is unrecognized, `normMatchStatus('text')` defaults to `'not_found'` (line 61), which then maps to `'NOT_FOUND_IN_REF'` in `llmMatchToDb()`, even if the actual result was "match" or "partial match."

---
