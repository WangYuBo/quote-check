# SubAgent 对话记录

> 项目: quote-check
> Session: 60a4fdd2-ceb2-4b32-8137-17d8a8c67cd3
> 时区: Asia/Shanghai (+08:00)

---

## 👤 User [13:28:56 +08:00]

Design a plan to fix two issues in the quote-check app:

## Issue 1: Production Blob upload hangs on "正在解析…"
- `lib/storage/blob.ts` uses `{ access: 'public' }` for Vercel Blob put()
- The Vercel Blob store is configured as private, so `access: 'public'` fails
- In `app/api/manuscripts/route.ts`, the Blob upload is NOT wrapped in try-catch
- When Blob upload throws, Next.js returns an HTML 500 error page (not JSON)
- The upload page client tries `res.json()` on an HTML response → hangs forever
- `app/api/references/route.ts` also uses the same blob upload without NODE_ENV guard (it only checks for placeholder token)
- `inngest/functions/ttl-destroy.ts` deletes blobs by URL, it checks for `local://` prefix

## Issue 2: /upload page accessible without login
- All data API routes check `auth.api.getSession()` and return 401
- But `/upload` page.tsx (client component) has NO session check
- Unauthenticated users can reach the page and see the upload UI
- API calls fail with 401, showing a generic error
- `/login` page was created as a basic sign-in/sign-up form
- The root page links to `/upload` instead of `/login`
- Better Auth client is set up in `lib/auth-client.ts` as `createAuthClient({ baseURL: window.location.origin })`
- No middleware.ts exists

## Files involved:
- `lib/storage/blob.ts` - change `access: 'public'` to `access: 'private'`
- `app/api/manuscripts/route.ts` - add try-catch around blob upload, fall back to local://
- `app/api/references/route.ts` - add NODE_ENV guard and try-catch for blob upload
- `app/upload/page.tsx` - add session check, redirect to /login if unauthenticated
- `app/page.tsx` (root) - consider adding login link

Provide a detailed step-by-step plan with exact code changes needed.

---

## 🤖 Assistant [13:29:01 +08:00]

<details>
<summary>💭 Thinking</summary>

```
The user wants me to design a plan to fix two issues in the quote-check app. Let me explore the codebase thoroughly to understand the current state of all the files mentioned and any related code.

Let me start by reading all the key files mentioned.
```

</details>

---

## 🤖 Assistant [13:29:01 +08:00]

I will start by reading all the relevant files to understand the current code before designing the plan.

---

## 🤖 Assistant [13:29:02 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/lib/storage/blob.ts"
}
```

📤 Result:

```
1	import { del, put } from '@vercel/blob';
2	
3	async function uploadToBlob(
4	  path: string,
5	  buffer: Buffer,
6	  mimeType: string,
7	): Promise<{ url: string; pathname: string }> {
8	  const blob = await put(path, buffer, { access: 'public', contentType: mimeType });
9	  return { url: blob.url, pathname: blob.pathname };
10	}
11	
12	export async function uploadManuscriptBlob(
13	  filename: string,
14	  buffer: Buffer,
15	  mimeType: string,
16	): Promise<{ url: string; pathname: string }> {
17	  return uploadToBlob(`manuscripts/${Date.now()}-${filename}`, buffer, mimeType);
18	}
19	
20	export async function uploadReferenceBlob(
21	  filename: string,
22	  buffer: Buffer,
23	  mimeType: string,
24	): Promise<{ url: string; pathname: string }> {
25	  return uploadToBlob(`references/${Date.now()}-${filename}`, buffer, mimeType);
26	}
27	
28	export async function deleteBlobByUrl(url: string): Promise<void> {
29	  await del(url);
30	}
31	
```

---

## 🤖 Assistant [13:29:02 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/api/manuscripts/route.ts"
}
```

📤 Result:

```
1	import type { NextRequest } from 'next/server';
2	import { NextResponse } from 'next/server';
3	
4	import { auth } from '@/lib/auth';
5	import { parseManuscript } from '@/lib/parsers/manuscript';
6	import { createManuscript, markManuscriptParsed, saveParagraphs } from '@/lib/services/manuscript';
7	import { uploadManuscriptBlob } from '@/lib/storage/blob';
8	
9	const ALLOWED_MIME = new Set([
10	  'text/plain',
11	  'text/markdown',
12	  'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
13	]);
14	const MAX_SIZE = 20 * 1024 * 1024; // 20 MB
15	
16	export async function POST(req: NextRequest) {
17	  const session = await auth.api.getSession({ headers: req.headers });
18	  if (!session?.user?.id) {
19	    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
20	  }
21	
22	  let formData: FormData;
23	  try {
24	    formData = await req.formData();
25	  } catch {
26	    return NextResponse.json({ error: '无法解析 form-data' }, { status: 400 });
27	  }
...(truncated)
```

---

## 🤖 Assistant [13:29:03 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/api/references/route.ts"
}
```

📤 Result:

```
1	import type { NextRequest } from 'next/server';
2	import { NextResponse } from 'next/server';
3	
4	import { auth } from '@/lib/auth';
5	import { parseManuscript } from '@/lib/parsers/manuscript';
6	import {
7	  createReference,
8	  listUserReferences,
9	  markReferenceParsed,
10	  saveReferenceParagraphs,
11	} from '@/lib/services/reference';
12	import { uploadReferenceBlob } from '@/lib/storage/blob';
13	
14	const ALLOWED_MIME = new Set([
15	  'text/plain',
16	  'text/markdown',
17	  'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
18	]);
19	const MAX_SIZE = 20 * 1024 * 1024;
20	const ALLOWED_ROLES = new Set(['CANON', 'ANNOTATED', 'TRANSLATED', 'TOOL', 'OTHER'] as const);
21	
22	export async function POST(req: NextRequest) {
23	  const session = await auth.api.getSession({ headers: req.headers });
24	  if (!session?.user?.id) {
25	    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
26	  }
27	
28	  let formData: FormData;
29	  try {
30	...(truncated)
```

---

## 🤖 Assistant [13:29:03 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/upload/page.tsx"
}
```

📤 Result:

```
1	'use client';
2	
3	import { useRouter } from 'next/navigation';
4	import { useRef, useState } from 'react';
5	
6	const ALLOWED_EXT = ['.txt', '.md', '.docx'];
7	const MAX_SIZE = 20 * 1024 * 1024;
8	const MAX_REFS = 10;
9	
10	interface RefItem {
11	  file: File;
12	  canonicalName: string;
13	  role: 'CANON' | 'ANNOTATED' | 'TRANSLATED' | 'TOOL' | 'OTHER';
14	  referenceId: string | null;
15	  uploading: boolean;
16	  error: string | null;
17	}
18	
19	export default function UploadPage() {
20	  const router = useRouter();
21	  const manuscriptInputRef = useRef<HTMLInputElement>(null);
22	  const refInputRef = useRef<HTMLInputElement>(null);
23	  const [dragging, setDragging] = useState(false);
24	  const [status, setStatus] = useState<'idle' | 'uploading' | 'creating' | 'error'>('idle');
25	  const [errorMsg, setErrorMsg] = useState('');
26	  const [preview, setPreview] = useState<{
27	    filename: string;
28	    paragraphCount: number;
29	    charCount: number;
30	  } | null>(null);...(truncated)
```

---

## 🤖 Assistant [13:29:04 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/page.tsx"
}
```

📤 Result:

```
1	import Link from 'next/link';
2	
3	export default function HomePage() {
4	  return (
5	    <main className="mx-auto max-w-3xl px-6 py-16">
6	      <header className="mb-8">
7	        <p className="text-sm text-[var(--color-fg-muted)]">v1.0</p>
8	        <h1 className="mt-2 font-serif text-4xl font-bold">引用核查</h1>
9	        <p className="mt-3 text-lg text-[var(--color-fg-muted)]">
10	          文史类书稿的三维度引用比对：字词、解释、语境。
11	          <br />
12	          机器不判错，只报"一致 / 部分一致 / 不一致 / 未找到"。终审权归编辑。
13	        </p>
14	      </header>
15	
16	      <div className="mt-6">
17	        <Link
18	          href="/upload"
19	          className="inline-block px-6 py-3 rounded-xl bg-[var(--color-primary)] text-[var(--color-primary-fg)] font-medium text-sm hover:opacity-90 transition-opacity"
20	        >
21	          上传书稿，开始核查
22	        </Link>
23	      </div>
24	
25	      <footer className="mt-12 text-xs text-[var(--color-fg-muted)]">
26	        <p>机器初审候选 · 终审权归编辑 · 不判"错"只报"不符合"</p>
27	      </footer>
2...(truncated)
```

---

## 🤖 Assistant [13:29:04 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/inngest/functions/ttl-destroy.ts"
}
```

📤 Result:

```
1	/**
2	 * TTL 销毁（MAS-6 · MS-D-05 · real.md #3）
3	 *
4	 * 每 10 分钟扫描 ttl_expires_at 已过期且 destroyedAt=null 的任务，
5	 * 删除 Vercel Blob 上的书稿 + 参考文件，标记 destroyed_at。
6	 *
7	 * 不删除 verification_result / report_snapshot（real.md #7 版本冻结原则）。
8	 */
9	import { and, eq, isNull, lt } from 'drizzle-orm';
10	
11	import { inngest } from '@/inngest/client';
12	import { logger } from '@/lib/logger';
13	import { db } from '@/lib/db';
14	import { manuscript, reference, task } from '@/lib/db/schema';
15	import { deleteBlobByUrl } from '@/lib/storage/blob';
16	
17	export const ttlDestroyFn = inngest.createFunction(
18	  {
19	    id: 'ttl-destroy',
20	    name: 'TTL 销毁 · 过期书稿清理',
21	    concurrency: { limit: 1 },
22	    triggers: [{ cron: '*/10 * * * *' }],
23	  },
24	  async ({ step }) => {
25	    const expired = await step.run('scan-expired', async () => {
26	      return db
27	        .select({ id: task.id, manuscriptId: task.manuscriptId })
28	        .from(task)
29	        .where(and(lt(task.ttlExpiresAt,...(truncated)
```

---

## 🤖 Assistant [13:29:05 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/lib/auth-client.ts"
}
```

📤 Result:

```
1	import { createAuthClient } from 'better-auth/client';
2	
3	export const authClient = createAuthClient({
4	  baseURL: typeof window !== 'undefined' ? window.location.origin : '',
5	});
6	
```

---

## 🤖 Assistant [13:29:07 +08:00]

<details>
<summary>💭 Thinking</summary>

```
Let me now explore some additional related files to fully understand the architecture before writing the plan.
```

</details>

---

## 🤖 Assistant [13:29:07 +08:00]

Let me also read some additional related files for context.

---

## 🤖 Assistant [13:29:08 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/login/page.tsx"
}
```

📤 Result:

```
1	'use client';
2	
3	import { useState } from 'react';
4	import { useRouter } from 'next/navigation';
5	
6	export default function LoginPage() {
7	  const router = useRouter();
8	  const [mode, setMode] = useState<'signin' | 'signup'>('signin');
9	  const [email, setEmail] = useState('');
10	  const [password, setPassword] = useState('');
11	  const [name, setName] = useState('');
12	  const [msg, setMsg] = useState('');
13	  const [loading, setLoading] = useState(false);
14	
15	  async function handleSubmit(e: React.FormEvent) {
16	    e.preventDefault();
17	    setLoading(true);
18	    setMsg('');
19	
20	    const endpoint = mode === 'signin' ? '/api/auth/sign-in/email' : '/api/auth/sign-up/email';
21	    const body: Record<string, string> = { email, password };
22	    if (mode === 'signup') body['name'] = name;
23	
24	    const res = await fetch(endpoint, {
25	      method: 'POST',
26	      headers: { 'Content-Type': 'application/json' },
27	      body: JSON.stringify(body),
28	    ...(truncated)
```

---

## 🤖 Assistant [13:29:08 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/layout.tsx"
}
```

📤 Result:

```
1	import type { Metadata } from 'next';
2	import type { ReactNode } from 'react';
3	import './globals.css';
4	
5	import AgreementModal from '@/components/AgreementModal';
6	
7	export const metadata: Metadata = {
8	  title: '引用核查 · 文史类书稿三维度比对',
9	  description: '对照原始文献，从字词、解释、语境三维度比对书稿引用。机器不判错，终审权归编辑。',
10	};
11	
12	export default function RootLayout({ children }: { children: ReactNode }) {
13	  return (
14	    <html lang="zh-CN" data-skin="C" suppressHydrationWarning>
15	      <body className="min-h-screen antialiased">
16	        {children}
17	        <AgreementModal />
18	      </body>
19	    </html>
20	  );
21	}
22	
```

---

## 🤖 Assistant [13:29:09 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la /Users/wangyubo/github-resp/quote-check/app/"
}
```

📤 Result:

```
api/
history/
login/
reports/
tasks/
upload/
globals.css  2.5K
layout.tsx  676B
page.tsx  1.1K

📊 3 files, 6 dirs (2 .tsx, 1 .css)
```

---

## 🤖 Assistant [13:29:10 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la /Users/wangyubo/github-resp/quote-check/middleware.ts 2>/dev/null; echo \"---\"; ls -la /Users/wangyubo/github-resp/quote-check/src/middleware.ts 2>/dev/null"
}
```

❌ Error:

```
Exit code 1
---
```

---

## 🤖 Assistant [13:29:12 +08:00]

<details>
<summary>💭 Thinking</summary>

```
Let me also look at the auth library to understand the Better Auth setup and how session checking works in the app, and the tasks API route.
```

</details>

---

## 🤖 Assistant [13:29:12 +08:00]

Let me look at a few more files for the auth setup and see how other routes handle error responses.

---

## 🤖 Assistant [13:29:13 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/lib/auth.ts"
}
```

📤 Result:

```
1	import { betterAuth } from 'better-auth';
2	import { drizzleAdapter } from 'better-auth/adapters/drizzle';
3	
4	import { db, schema } from '@/lib/db';
5	import { env } from '@/lib/env';
6	
7	/**
8	 * Better Auth 服务端实例
9	 *
10	 * spec-system-architecture ADR-004 · spec-product-requirements MS-L-01/02 · MS-D-01
11	 *
12	 * 设计要点：
13	 *   - emailAndPassword：MVP 阶段启用；requireEmailVerification 先关，避免首次冒烟被邮件阻塞
14	 *   - drizzleAdapter(db, {provider:'pg', schema})：复用 lib/db/schema.ts 的 user/session/account/verification 四表
15	 *   - generateId: false：用 PG 的 gen_random_uuid() 作为 id（schema.user.id 已是 uuid + defaultRandom()）
16	 *   - session.expiresIn: 7d（C 端默认；B 端差异化通过后续 hook 在 signIn 时覆盖 expiresAt）
17	 *   - user.additionalFields.role：'B' | 'C' | 'admin'，input:false 防止用户自行提升权限
18	 *
19	 * 不在这里做的（按 spec 锁定）：
20	 *   - requireEmailVerification = true：进入正式邀请内测前开启
21	 *   - RBAC 插件（admin 授权）：MS-D-01 开发时加 plugin
22	 *   - B 端 4h 会话差异化：进入 MS-L-02 时通过 signIn hook 覆盖
23	 *   - OAuth provider：v1.0 只做 em...(truncated)
```

---

## 🤖 Assistant [13:29:13 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/api/tasks/route.ts"
}
```

📤 Result:

```
1	import type { NextRequest } from 'next/server';
2	import { NextResponse } from 'next/server';
3	
4	import { inngest } from '@/inngest/client';
5	import { auth } from '@/lib/auth';
6	import {
7	  COST_CONFIRM_THRESHOLD_FEN,
8	  DEFAULT_COST_CEILING_FEN,
9	  estimateCostFen,
10	  formatFenAsYuan,
11	} from '@/lib/ai/cost';
12	import { getManuscript } from '@/lib/services/manuscript';
13	import { createTask, updateTaskStatus } from '@/lib/services/task';
14	
15	export async function POST(req: NextRequest) {
16	  const session = await auth.api.getSession({ headers: req.headers });
17	  if (!session?.user?.id) {
18	    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
19	  }
20	
21	  let body: { manuscriptId?: string; referenceIds?: string[]; costConfirmed?: boolean };
22	  try {
23	    body = (await req.json()) as typeof body;
24	  } catch {
25	    return NextResponse.json({ error: '无效 JSON' }, { status: 400 });
26	  }
27	
28	  if (!body.manuscriptId) {
29	    return ...(truncated)
```

---

## 🤖 Assistant [13:29:14 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/package.json"
}
```

📤 Result:

```
1	{
2	  "name": "quote-check",
3	  "version": "1.0.0-scaffold",
4	  "private": true,
5	  "description": "文史类引用校对软件 v1.0 — 三维度核查（字词/解释/语境），编辑决定、机器不判错",
6	  "engines": {
7	    "node": ">=20.11"
8	  },
9	  "packageManager": "bun@1.1.38",
10	  "scripts": {
11	    "dev": "next dev",
12	    "build": "next build",
13	    "start": "next start",
14	    "typecheck": "tsc --noEmit",
15	    "lint": "eslint .",
16	    "lint:fix": "eslint . --fix",
17	    "format": "prettier --write .",
18	    "format:check": "prettier --check .",
19	    "test": "vitest run",
20	    "test:watch": "vitest",
21	    "test:coverage": "vitest run --coverage",
22	    "test:unit": "vitest run tests/unit",
23	    "test:contract": "DOCKER_HOST=${DOCKER_HOST:-unix:///Users/wangyubo/.docker/run/docker.sock} vitest run tests/contract",
24	    "test:compliance": "vitest run tests/compliance",
25	    "test:prompts": "vitest run tests/compliance/prompt-integrity.test.ts",
26	    "test:neutral": "vitest run tests/compliance/neutral...(truncated)
```

---

## 🤖 Assistant [13:29:16 +08:00]

<details>
<summary>💭 Thinking</summary>

```
Now let me also check the history and reports routes for auth pattern reference, and also see if there's a next.config file to understand the env config.
```

</details>

---

## 🤖 Assistant [13:29:16 +08:00]

Let me check a couple more files for patterns and the environment configuration.

---

## 🤖 Assistant [13:29:17 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/api/history/route.ts"
}
```

❌ Error:

```
File does not exist. Note: your current working directory is /Users/wangyubo/github-resp/quote-check.
```

---

## 🤖 Assistant [13:29:17 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/lib/env.ts"
}
```

📤 Result:

```
1	import { z } from 'zod';
2	
3	// spec-coding §19 单一校验出口
4	// 应用代码禁止直接 process.env.X；必须从此文件 import env
5	//
6	// 严格模式：非 test 环境下启动失败 = 真失败（不给静默占位）
7	// test 环境允许占位，便于 CI 在不配 DB 的前提下跑 typecheck/lint
8	
9	const envSchema = z.object({
10	  NODE_ENV: z.enum(['development', 'test', 'production']).default('development'),
11	  DATABASE_URL: z.string().url(),
12	  BETTER_AUTH_SECRET: z.string().min(32),
13	  BETTER_AUTH_URL: z.string().url(),
14	  SILICONFLOW_API_KEY: z.string().min(20),
15	  BLOB_READ_WRITE_TOKEN: z.string().min(20),
16	  INNGEST_EVENT_KEY: z.string().min(10),
17	  INNGEST_SIGNING_KEY: z.string().min(10),
18	  LOG_LEVEL: z.enum(['trace', 'debug', 'info', 'warn', 'error']).default('info'),
19	  COST_CAP_CNY: z.coerce.number().positive().default(50), // real.md #6
20	  TTL_DAYS: z.coerce.number().int().positive().default(7), // real.md #3
21	  DEMO_MODE: z
22	    .enum(['true', 'false'])
23	    .default('false')
24	    .transform((v) => v === 'true'),
25	});
26	
27	type Env = ...(truncated)
```

---

## 🤖 Assistant [13:29:18 +08:00]

### 🔧 Bash

```json
{
  "command": "ls /Users/wangyubo/github-resp/quote-check/app/api/"
}
```

📤 Result:

```
admin/
agreement/
auth/
inngest/
manuscripts/
references/
reports/
tasks/

📊 0 files, 8 dirs
```

---

## 🤖 Assistant [13:29:19 +08:00]

<details>
<summary>💭 Thinking</summary>

```
Now let me look at how the reports API route handles auth for comparison, and also check if there's any Next.js config file.
```

</details>

---

## 🤖 Assistant [13:29:20 +08:00]

Let me check the Next.js config and one more API route for patterns.

---

## 🤖 Assistant [13:29:20 +08:00]

### 🔧 Bash

```json
{
  "command": "ls /Users/wangyubo/github-resp/quote-check/next.config.* 2>/dev/null; ls /Users/wangyubo/github-resp/quote-check/app/api/reports/ 2>/dev/null"
}
```

📤 Result:

```
/Users/wangyubo/github-resp/quote-check/next.config.ts  241B

📊 1 files, 0 dirs (1 .ts)
[taskId]
```

---

## 🤖 Assistant [13:29:21 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/app/api/reports/route.ts"
}
```

❌ Error:

```
File does not exist. Note: your current working directory is /Users/wangyubo/github-resp/quote-check.
```

---

## 🤖 Assistant [13:29:23 +08:00]

<details>
<summary>💭 Thinking</summary>

```
Let me look at the next.config.ts and the reports route file (it might be in the path with a subdirectory).
```

</details>

---

## 🤖 Assistant [13:29:23 +08:00]

### 🔧 Read

```json
{
  "file_path": "/Users/wangyubo/github-resp/quote-check/next.config.ts"
}
```

📤 Result:

```
1	import type { NextConfig } from 'next';
2	
3	const nextConfig: NextConfig = {
4	  reactStrictMode: true,
5	  poweredByHeader: false,
6	  serverExternalPackages: ['pino', 'pino-pretty', 'opencc-js'],
7	  typedRoutes: true,
8	};
9	
10	export default nextConfig;
11	
```

---

## 🤖 Assistant [13:29:24 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la /Users/wangyubo/github-resp/quote-check/app/api/reports/"
}
```

📤 Result:

```
[taskId]/

📊 0 files, 1 dirs
```

---

## 🤖 Assistant [13:30:15 +08:00]

<details>
<summary>💭 Thinking</summary>

```
Now I have a thorough understanding of the codebase. Let me also quickly check the `@vercel/blob` documentation for the `put()` API to understand the `access` option behavior for private stores. But more importantly, let me now construct my detailed plan.

Let me summarize what I've found:

## Issue 1: Production Blob upload hangs on "正在解析..."

### Root cause chain:
1. `lib/storage/blob.ts` line 8: `put(path, buffer, { access: 'public', contentType: mimeType })` uses `access: 'public'`
2. The Vercel Blob store is configured as private, so `access: 'public'` causes the `put()` call to throw an error
3. In `app/api/manuscripts/route.ts` lines 62-67: The blob upload is NOT wrapped in try-catch. When it throws, there's no error handling.
4. When an unhandled error is thrown in a Next.js Route Handler, Next.js returns an HTML 500 error page (not JSON)
5. In `app/upload/page.tsx` line 61: `const data = (await res.json()) as ...` - the client tries to parse the HTML response as JSON, which hangs because the response isn't valid JSON and `res.json()` never resolves (or throws, but it's caught by the caller checking `res.ok`)
6. Actually, looking more carefully: `res.json()` will reject or hang? Let me think. If the response is HTML, `res.json()` would throw a JSON.parse error (SyntaxError), but the try-catch in `handleManuscriptFile` doesn't wrap it — it's just an async function that gets called and awaited. However, `res.json()` on a non-JSON response actually rejects with a SyntaxError. Looking at the code more carefully:

```js
const res = await fetch('/api/manuscripts', { method: 'POST', body: fd });
const data = (await res.json()) as {...};
```

If `res.json()` throws, the whole async function `handleManuscriptFile` will throw, and since it's called as `void handleManuscriptFile(file)` the error is unhandled (there's no `.catch()` on the returned promise). This means the rejection is unhandled.

But wait - the issue description says "hangs forever". Let me think again....(truncated)
```

</details>

---

## 🤖 Assistant [13:30:16 +08:00]

Now let me check the versions endpoint and also see if there are any tests related to storage.

---

## 🤖 Assistant [13:30:17 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -la /Users/wangyubo/github-resp/quote-check/app/api/reports/"
}
```

📤 Result:

```
[taskId]/

📊 0 files, 1 dirs
```

---

## 🤖 Assistant [13:30:17 +08:00]

### 🔧 Bash

```json
{
  "command": "find /Users/wangyubo/github-resp/quote-check -name \"*.test.*\" -o -name \"*.spec.*\" | head -20"
}
```

📤 Result:

```
error: unexpected argument '-n' found

  tip: to pass '-n' as a value, use '-- -n'

Usage: rtk find [OPTIONS] <PATTERN> [PATH]

For more information, try '--help'.
```

---

## 🤖 Assistant [13:30:19 +08:00]

<details>
<summary>💭 Thinking</summary>

```
Let me fix the find command syntax.
```

</details>

---

## 🤖 Assistant [13:30:20 +08:00]

### 🔧 Bash

```json
{
  "command": "find /Users/wangyubo/github-resp/quote-check -name \"*.test.*\" -o -name \"*.spec.*\" 2>/dev/null"
}
```

❌ Error:

```
Exit code 2
```

---

## 🤖 Assistant [13:30:21 +08:00]

<details>
<summary>💭 Thinking</summary>

```
Let me try a different approach to find test files.
```

</details>

---

## 🤖 Assistant [13:30:22 +08:00]

### 🔧 Bash

```json
{
  "command": "find /Users/wangyubo/github-resp/quote-check -type f -name \"*.test.*\" -o -type f -name \"*.spec.*\" 2>/dev/null"
}
```

❌ Error:

```
Exit code 2
```

---

## 🤖 Assistant [13:30:25 +08:00]

<details>
<summary>💭 Thinking</summary>

```
Seems like the find command on macOS is choking on something. Let me try a different approach.
```

</details>

---

## 🤖 Assistant [13:30:25 +08:00]

### 🔧 Bash

```json
{
  "command": "find /Users/wangyubo/github-resp/quote-check/tests -type f 2>/dev/null | head -20 ; echo \"---\" ; ls /Users/wangyubo/github-resp/quote-check/tests 2>/dev/null"
}
```

📤 Result:

```
0 for '/Users/wangyubo/github-resp/quote-check/tests'
---
compliance
contract
e2e
fixtures
unit
```

---

## 🤖 Assistant [13:30:27 +08:00]

<details>
<summary>💭 Thinking</summary>

```
OK, let me see the unit tests.
```

</details>

---

## 🤖 Assistant [13:30:28 +08:00]

### 🔧 Bash

```json
{
  "command": "ls -R /Users/wangyubo/github-resp/quote-check/tests/unit/ 2>/dev/null"
}
```

📤 Result:

```
.gitkeep  0B
ai-confidence.test.ts  3.1K
idempotency.test.ts  2.7K

📊 3 files, 0 dirs (2 .ts, 1 .gitkeep)
```

---

## 🤖 Assistant [13:30:29 +08:00]

### 🔧 Bash

```json
{
  "command": "ls /Users/wangyubo/github-resp/quote-check/tests/contract/ 2>/dev/null"
}
```

📤 Result:

```
.gitkeep  0B
db-triggers.test.ts  16.7K

📊 2 files, 0 dirs (1 .gitkeep, 1 .ts)
```

---

## 🤖 Assistant [13:30:31 +08:00]

<details>
<summary>💭 Thinking</summary>

```
Good, now I have a thorough understanding of the codebase. Let me write the comprehensive plan.
```

</details>

---

## 🤖 Assistant [13:31:12 +08:00]

I have now thoroughly explored all relevant files. Here is the detailed implementation plan.

---

## Implementation Plan

### Overview

Two distinct issues need fixing: (1) the production Blob upload uses `access: 'public'` on a private store, throwing an unhandled error that causes Next.js to return HTML 500, making the client hang on "正在解析..."; (2) the `/upload` page is unprotected (no session check), so unauthenticated users can reach it and see a broken UI.

The fixes are independent but touch related files, so the ordering below groups them logically.

---

### Issue 1: Production Blob upload hangs on "正在解析..."

**Root cause chain:**

1. `lib/storage/blob.ts` passes `{ access: 'public' }` to `put()`, but the Vercel Blob store is configured for private access -- the SDK throws.
2. `app/api/manuscripts/route.ts` has no try-catch around `uploadManuscriptBlob()`, so the thrown error propagates unhandled.
3. Next.js Route Handlers return an HTML 500 page by default when an unhandled error is thrown.
4. The client (`app/upload/page.tsx`) calls `res.json()` on this HTML response, which throws a `SyntaxError` (no `.catch()` on the promise returned by `void handleManuscriptFile(file)`, so the error is silently swallowed by the runtime).
5. The UI state remains `'uploading'` with `"正在解析..."` displayed forever.
6. `app/api/references/route.ts` has the same gap: `uploadReferenceBlob()` is called without try-catch and without a `NODE_ENV !== 'development'` guard (unlike the manuscripts route).

**Fixes for Issue 1:**

#### Step 1a -- `lib/storage/blob.ts`: Change `access` to `'private'`

**File:** `/Users/wangyubo/github-resp/quote-check/lib/storage/blob.ts`

**Change:** On line 8, replace `access: 'public'` with `access: 'private'`.

```diff
- const blob = await put(path, buffer, { access: 'public', contentType: mimeType });
+ const blob = await put(path, buffer, { access: 'private', contentType: mimeType });
```

**Rationale:** This is the direct cause. The Vercel Blob store does not permit public access; `access: 'private'` matches the store's configuration. The `del()` call in `deleteBlobByUrl` does not pass an `access` option, so it works with either mode because it relies on the `BLOB_READ_WRITE_TOKEN` for authorization.

**Verification:** After this change, `put()` will succeed in production. The `ttl-destroy.ts` function already checks `!msRow.blobUrl.startsWith('local://')` before calling `deleteBlobByUrl`, so it will correctly handle `local://` fallback URLs and attempt deletion of real blob URLs.

#### Step 1b -- `app/api/manuscripts/route.ts`: Wrap blob upload in try-catch

**File:** `/Users/wangyubo/github-resp/quote-check/app/api/manuscripts/route.ts`

**Change:** Wrap the blob upload block (lines 59-67) in a try-catch so that if the Blob API ever fails again (network blip, auth rotation, etc.), the request still succeeds with a `local://` fallback URL.

Replace lines 59-67:

```diff
   // 2. 上传到 Vercel Blob（本地开发 / token 无效时跳过）
   let url = `local://manuscripts/${Date.now()}-${file.name}`;
   let pathname = url;
   const blobToken = process.env['BLOB_READ_WRITE_TOKEN'] ?? '';
-  if (!blobToken.includes('placeholder') && process.env['NODE_ENV'] !== 'development') {
-    const uploaded = await uploadManuscriptBlob(file.name, buffer, mimeType);
-    url = uploaded.url;
-    pathname = uploaded.pathname;
+  if (!blobToken.includes('placeholder') && process.env['NODE_ENV'] !== 'development') {
+    try {
+      const uploaded = await uploadManuscriptBlob(file.name, buffer, mimeType);
+      url = uploaded.url;
+      pathname = uploaded.pathname;
+    } catch (err) {
+      console.warn('[manuscripts] Blob upload failed, falling back to local://', err);
+    }
   }
```

**Rationale:** If `uploadManuscriptBlob` throws for any reason, the code catches it, logs a warning, and keeps the `local://` fallback URL that was already set. The manuscript record is still created and parsing is preserved. Without this, any Blob failure makes the entire upload fail.

#### Step 1c -- `app/api/references/route.ts`: Add `NODE_ENV` guard and try-catch

**File:** `/Users/wangyubo/github-resp/quote-check/app/api/references/route.ts`

Two changes needed:

**1c-i:** Add the missing `process.env['NODE_ENV'] !== 'development'` guard to match the pattern in the manuscripts route. This prevents Blob upload attempts during local development when the developer's token might be a placeholder.

Replace lines 87-92:

```diff
   let url = `local://references/${Date.now()}-${file.name}`;
   let pathname = url;
   const blobToken = process.env['BLOB_READ_WRITE_TOKEN'] ?? '';
-  if (!blobToken.includes('placeholder')) {
-    const uploaded = await uploadReferenceBlob(file.name, buffer, mimeType);
-    url = uploaded.url;
-    pathname = uploaded.pathname;
+  if (!blobToken.includes('placeholder') && process.env['NODE_ENV'] !== 'development') {
+    try {
+      const uploaded = await uploadReferenceBlob(file.name, buffer, mimeType);
+      url = uploaded.url;
+      pathname = uploaded.pathname;
+    } catch (err) {
+      console.warn('[references] Blob upload failed, falling back to local://', err);
+    }
   }
```

**Rationale:** Same reasons as Step 1b, plus the symmetry with the manuscripts route.

#### Step 1d -- `app/upload/page.tsx`: Guard against non-JSON responses on the client

**File:** `/Users/wangyubo/github-resp/quote-check/app/upload/page.tsx`

This is a defensive layer. Even with server-side fixes in place, if a proxy, reverse proxy, or future code change produces a non-JSON response, the client should degrade gracefully instead of hanging.

Two locations need protection:

**1d-i:** `handleManuscriptFile` (lines 60-63). Change:

```diff
     const res = await fetch('/api/manuscripts', { method: 'POST', body: fd });
-    const data = (await res.json()) as {
-      manuscriptId?: string; paragraphCount?: number; charCount?: number; error?: string;
-    };
-    if (!res.ok) { setErrorMsg(data.error ?? '上传失败'); setStatus('error'); return; }
+    let data: { manuscriptId?: string; paragraphCount?: number; charCount?: number; error?: string };
+    try {
+      data = await res.json() as typeof data;
+    } catch {
+      data = {};
+    }
+    if (!res.ok) { setErrorMsg(data.error ?? '上传失败'); setStatus('error'); return; }
```

**1d-ii:** `uploadRef` (lines 105-106). Change:

```diff
     const res = await fetch('/api/references', { method: 'POST', body: fd });
-    const data = (await res.json()) as { referenceId?: string; error?: string };
-    if (!res.ok) {
+    let data: { referenceId?: string; error?: string };
+    try {
+      data = await res.json() as typeof data;
+    } catch {
+      data = {};
+    }
+    if (!res.ok) {
-      setRefs((prev) => prev.map((r, i) => i === idx ? { ...r, uploading: false, error: data.error ?? '上传失败' } : r));
+      setRefs((prev) => prev.map((r, i) => i === idx ? { ...r, uploading: false, error: data.error ?? '上传失败' } : r));
     } else {
-      setRefs((prev) => prev.map((r, i) => i === idx ? { ...r, uploading: false, referenceId: data.referenceId ?? null } : r));
+      setRefs((prev) => prev.map((r, i) => i === idx ? { ...r, uploading: false, referenceId: data.referenceId ?? null } : r));
     }
```

Note the diff here is actually only adding the try-catch block around `res.json()`; the `setRefs` lines remain unchanged structurally.

**Rationale:** If `res.json()` throws (non-JSON body), `data` becomes an empty object, and the `!res.ok` branch will show the generic error message "上传失败". This prevents the forever-hanging "正在解析..." / "上传中..." states.

---

### Issue 2: `/upload` page accessible without login

**Current state:**
- All API routes (`/api/manuscripts`, `/api/references`, `/api/tasks`) check `auth.api.getSession()` and return 401.
- `/upload/page.tsx` is a client component with no session check.
- `/login/page.tsx` exists as a functional sign-in/sign-up form.
- `lib/auth-client.ts` has `createAuthClient` configured.
- No `middleware.ts` file exists.
- Root `page.tsx` links directly to `/upload` instead of `/login`.

**Approach:** Add a client-side session check in `/upload/page.tsx` using the existing `authClient`. This approach:
- Reuses the already-set-up Better Auth client.
- Avoids adding middleware (which would be a larger architectural change).
- Redirects unauthenticated users to `/login` before they see any upload UI.

#### Step 2a -- `app/upload/page.tsx`: Add session check

**File:** `/Users/wangyubo/github-resp/quote-check/app/upload/page.tsx`

Add the import for `authClient` and `useEffect`:

```diff
+import { useEffect, useState } from 'react';
 import { useRouter } from 'next/navigation';
-import { useRef, useState } from 'react';
+import { useRef } from 'react';
+import { authClient } from '@/lib/auth-client';
```

Then add a session-loading guard at the top of the component function, before the current `const [dragging, setDragging] = ...` state declarations:

```diff
 export default function UploadPage() {
   const router = useRouter();
+  const [sessionChecked, setSessionChecked] = useState(false);
+
+  useEffect(() => {
+    authClient.getSession().then(({ data }) => {
+      if (!data?.user) {
+        router.push('/login');
+      } else {
+        setSessionChecked(true);
+      }
+    });
+  }, [router]);
+
+  if (!sessionChecked) {
+    return (
+      <main className="min-h-screen bg-(--color-bg) flex items-center justify-center p-8">
+        <p className="text-(--color-fg-muted)">检查登录状态…</p>
+      </main>
+    );
+  }
+
   const [dragging, setDragging] = useState(false);
```

**Rationale:**
- On first mount, the `useEffect` calls `authClient.getSession()`. If no session exists, `router.push('/login')` navigates away immediately.
- Until the session check resolves, a brief loading message is shown. This prevents a flash of the upload UI for unauthenticated users.
- For authenticated users, `sessionChecked` becomes `true` and the upload UI renders normally.
- The `authClient` already handles cookie-based session detection (Better Auth stores sessions in cookies).

**Edge cases handled:**
- **Loading state**: A brief message is shown while the session check is in flight, preventing flash-of-unauthenticated-content.
- **Error state**: If `getSession()` throws (network error), the promise will reject. The `useEffect` callback should handle this by catching and redirecting to `/login` as a safe default. Use a try-catch inside the effect:

```ts
  useEffect(() => {
    authClient.getSession()
      .then(({ data }) => {
        if (!data?.user) {
          router.push('/login');
        } else {
          setSessionChecked(true);
        }
      })
      .catch(() => {
        router.push('/login');
      });
  }, [router]);
```

#### Step 2b -- `app/page.tsx`: Change link to point to `/login`

**File:** `/Users/wangyubo/github-resp/quote-check/app/page.tsx`

**Change:** On line 18, change `href="/upload"` to `href="/login"`.

```diff
-          href="/upload"
+          href="/login"
   className="inline-block px-6 py-3 rounded-xl bg-[var(--color-primary)] text-[var(--color-primary-fg)] font-medium text-sm hover:opacity-90 transition-opacity"
 >
   上传书稿，开始核查
```

**Rationale:** The root marketing page should send unauthenticated users through the login flow. After successful login, the `/login` page already redirects to `/upload` (line 46 of `/login/page.tsx`).

---

### Files NOT requiring changes

- **`inngest/functions/ttl-destroy.ts`**: Already correctly checks `!msRow.blobUrl.startsWith('local://')` before calling `del()`. The `local://` fallback URLs from the route handlers will be skipped correctly. No changes needed.

- **`lib/auth-client.ts`**: Already correctly configured. The `baseURL: typeof window !== 'undefined' ? window.location.origin : ''` pattern works for client-side session checking.

- **`lib/auth.ts` (server)**: No changes needed. API routes already use `auth.api.getSession()` correctly.

- **`app/api/tasks/route.ts`**: Already has proper auth checks and returns JSON. No changes needed.

---

### Implementation Order

The changes can be applied in any order since they touch non-overlapping files, but the recommended sequence is:

1. **`lib/storage/blob.ts`** (Step 1a) -- the root cause of the production failure.
2. **`app/api/manuscripts/route.ts`** (Step 1b) -- resilience for the upload endpoint.
3. **`app/api/references/route.ts`** (Step 1c) -- resilience for the references endpoint.
4. **`app/upload/page.tsx`** (Steps 1d + 2a) -- client improvements: JSON parse safety + session check.
5. **`app/page.tsx`** (Step 2b) -- root page link fix.

### Testing Guidance

- **Issue 1 verification**: Set `BLOB_READ_WRITE_TOKEN` to a valid production token, ensure the Vercel Blob store is private, then upload a manuscript. The upload should succeed. Then simulate a Blob failure by temporarily invalidating the token or making the store unreachable -- the upload should still succeed with a `local://` URL stored in the database.
- **Issue 2 verification**: Clear cookies / open an incognito window, navigate to `/upload`. Should redirect to `/login` immediately. After login, navigating to `/upload` should show the upload UI.

---

### Critical Files for Implementation

- `/Users/wangyubo/github-resp/quote-check/lib/storage/blob.ts`
- `/Users/wangyubo/github-resp/quote-check/app/api/manuscripts/route.ts`
- `/Users/wangyubo/github-resp/quote-check/app/api/references/route.ts`
- `/Users/wangyubo/github-resp/quote-check/app/upload/page.tsx`
- `/Users/wangyubo/github-resp/quote-check/app/page.tsx`

---
